#include "main.h"

/**
 * _abs - value absolute
 * @n: The integer to be computed.
 *
 * Return: The absolut value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
